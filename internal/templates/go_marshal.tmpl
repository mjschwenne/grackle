func Marshal(<<- param .Name >> *S, prefix []byte) []byte {
    var enc = prefix
    << range .Fields ->>
    enc = << if isRef . ->>
    << goType . | lower >>_gk.Marshal(<< param $.Name ->>.<< .Name >>, enc)
    << else ->>
    marshal.Write<<- marshalType . ->>(enc, << param $.Name ->>.<< .Name >>)
    << end ->><<- end ->>
    return enc
}
