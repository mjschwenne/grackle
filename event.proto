syntax = "proto3";

import "grackle.proto";
import "timestamp.proto";

// option (grackle.coq_logical_path) = "grackle.example";
// option (grackle.coq_physical_path) = "example";

message Event {
    int32 id = 1;
    TimeStamp startTime = 2;
    TimeStamp endTime = 3;
}
